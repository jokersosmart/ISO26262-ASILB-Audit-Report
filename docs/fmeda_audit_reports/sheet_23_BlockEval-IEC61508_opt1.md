_Generated by Manus, an autonomous general AI agent._

# FMEDA 深度稽核分析報告：BlockEval-IEC61508_opt1

**稽核專家：** Manus (ISO 26262 功能安全稽核專家)
**報告日期：** 2026-02-27

---

## 1. Sheet 概述

本章節提供 `BlockEval-IEC61508_opt1` 工作表的概觀，說明其在整體 FMEDA 分析中的角色與目的。

| 項目 | 內容 | 說明 |
| :--- | :--- | :--- |
| **Sheet 名稱** | `BlockEval-IEC61508_opt1` | 從名稱推斷，此工作表是基於 IEC 61508 標準對硬體「區塊」（Block）進行評估。後綴 `_opt1` 可能表示這是多個計算選項中的第一個。 |
| **報告中位置** | 第 23 個 Sheet | 在包含 27 個工作表的完整 FMEDA 報告中，此表位於後段，通常用於匯總和計算高階安全指標。 |
| **維度** | 201 行 × 36 列 (A1:AJ201) | 此維度提供了足夠的空間來分析多達 200 個硬體區塊，並進行詳細的失效率計算。 |
| **角色與目的** | **硬體架構指標計算** | 此工作表的**核心目的**是作為一個計算引擎，它從其他工作表（如 `BlockList`, `FMEDA`, `BlockEvaluation`）提取基礎失效率數據，並根據 IEC 61508 的指導原則，對每個硬體區塊進行安全指標的量化計算。其主要產出為**安全失效比例（SFF）**和**每小時危險失效機率（PFH）**這兩個關鍵的硬體架構指標。這些指標是判斷系統是否滿足特定安全完整性等級（SIL 或 ASIL）的直接依據。 |

## 2. 內容結構分析

本章節深入分析工作表的欄位設計與資料組織邏輯，以理解其計算流程。

### 欄位標頭的含義與組織方式

工作表的 36 個欄位（A 到 AJ）組織極具邏輯性，形成一個從左到右的計算流。其結構可分為以下幾個階段：

1.  **區塊識別 (A-C):** `No.`, `Block / Subblock name`, `Block / Component Type` - 識別分析的對象。
2.  **初始失效率 (D-L):** 從 `BlockList` 引入總失效率，並將其分解為**永久性 (permanent)**與**暫態性 (transient)**失效，再進一步區分為**安全相關 (Safety related)**與**非安全相關 (Not safety related)**。
3.  **安全相關失效率分佈 (M-U):** 將安全相關的失效率分解為**安全失效 (Safe)**與**危險失效 (Dangerous)**。
4.  **危險失效檢測分析 (V-AA):** 將危險失效率進一步分解為**可被檢測到的危險失效 (Dangerous Detected)**與**不可被檢測到的危險失效 (Dangerous Undetected)**。這是計算 SFF 的關鍵步驟。
5.  **硬體安全指標計算 (AB-AJ):** 基於前述的分解結果，最終計算出**安全失效比例 (SFF)** 和**每小時危險失效機率 (PFH)**。

這種結構清晰地展示了如何將一個總的失效率，根據其對安全的影響和是否能被安全機制檢測到，進行逐步細化和量化的過程。

### 合併儲存格的用途

根據提供的資料，此工作表**沒有使用任何合併儲存格**。這是一個良好的實踐，因為合併儲存格常常會導致公式複製和數據處理的困難，尤其是在複雜的計算表格中。

### 資料的組織邏輯

資料以**每行一個硬體區塊**的方式組織。每一行都是一個獨立的計算實體，從 `BlockList` 獲取標識符和基礎數據，然後通過一系列引用其他工作表（`FMEDA`, `BlockEvaluation`, `SafetyGoalViolations`）的公式，完成該區塊的 SFF 和 PFH 計算。這種高度關聯性的結構表明，此工作表是整個 FMEDA 分析的頂層匯總和結果呈現層。稽核時必須注意，**數據樣本中出現的大量 `#DIV/0!` 和 `#VALUE!` 錯誤**，這是一個嚴重的警訊，表明底層數據或公式存在問題，導致計算鏈中斷。

## 3. 圖片分析

根據提供的 JSON 資料，`BlockEval-IEC61508_opt1` 工作表中**不包含任何圖片**。因此，本章節不適用。

## 4. 公式深度分析

此工作表包含高達 7200 個公式，是 FMEDA 分析的核心計算引擎。以下列出幾個關鍵且具代表性的公式模式進行深度分析。

| 公式位置 | 公式範例 | 物理意義 / 計算目的 | 引用來源 | 對應標準 & 驗證要點 |
| :--- | :--- | :--- | :--- | :--- |
| **Y2** | `=SUMIFS(FMEDA!LR$4:LR$1653,FMEDA!$B$4:$B$1653,$B2)+BlockEvaluation!AE2` | 計算特定區塊 (B2) 的**永久性不可檢測危險失效率 (λ_DU,perm)**。它匯總了 `FMEDA` 表中歸屬於此區塊的所有相關失效模式的失效率，並可能加上 `BlockEvaluation` 表中的一個調整項。 | `FMEDA`, `BlockEvaluation` | **ISO 26262-5, Annex D/E; IEC 61508-2, Annex D**<br>**驗證要點：** `SUMIFS` 的條件範圍是否完整覆蓋所有相關失效模式？`BlockEvaluation!AE2` 的物理意義是什麼？是否重複計算？ |
| **Z2** | `=SUMIFS(FMEDA!LS$4:LS$1653,FMEDA!$B$4:$B$1653,$B2)+BlockEvaluation!AF2` | 計算特定區塊 (B2) 的**暫態性不可檢測危險失效率 (λ_DU,trans)**。邏輯與 Y2 相同，但針對暫態失效。 | `FMEDA`, `BlockEvaluation` | **ISO 26262-5, Annex F**<br>**驗證要點：** 暫態失效的計算模型是否合理？其對安全目標的影響是否已在 `SafetyGoalViolations` 表中正確評估？ |
| **AE2** | `=IF(G2=0,"-",IF(P2+S2=0,"Not calculated",(P2+V2)/(P2+S2)))` | 計算**永久性失效的安全失效比例 (SFF_perm)**。公式為 (λ_S + λ_DD) / (λ_S + λ_D)，即 (安全失效+可檢測危險失效) / (總安全相關失效)。 | `BlockEval-IEC61508_opt1` (自身) | **ISO 26262-5, 8.4.4; IEC 61508-2, 7.4.4.1**<br>**驗證要點：** 分母是否等於總的安全相關失效率 (G2)？`IF` 條件處理是否覆蓋所有邊界情況（如總失效率為零）？ |
| **AG2** | `=IF(I2=0,"-",IF(R2+U2=0,"Not calculated",(R2+X2)/(R2+U2)))` | 計算**永久性與暫態性失效的總安全失效比例 (SFF_total)**。這是評估硬體架構容錯能力的關鍵指標。 | `BlockEval-IEC61508_opt1` (自身) | **ISO 26262-5, 8.4.4**<br>**驗證要點：** SFF 的計算是否符合標準定義？是否達到目標 ASIL 等級的要求（例如 ASIL B 要求 SFF ≥ 90%）？ |
| **AH2** | `=(SUMIFS(FMEDA!LR$4:LR$1653,...) * SafetyGoalViolations!$F$37) + BlockEvaluation!AE2` | 計算**永久性失效的每小時危險失效機率 (PFH_perm)**。它將不可檢測的危險失效率乘以一個來自 `SafetyGoalViolations` 表的因子（可能是多點失效的機率），再加上一個調整項。 | `FMEDA`, `SafetyGoalViolations`, `BlockEvaluation` | **ISO 26262-5, 9.4.2; IEC 61508-2, 7.4.5**<br>**驗證要點：** `SafetyGoalViolations!$F$37` 的值是如何得出的？它是否正確反映了潛在失效（Latent Fault）的風險？PFH 的計算是否考慮了所有相關的危險失效？ |

## 5. 稽核合規性分析

此工作表是證明硬體設計滿足 ISO 26262 定量分析要求的核心證據。稽核員將對其進行極其嚴格的審查。

-   **對應的 ISO 26262 條款：**
    -   **Part 5, Clause 8 (Hardware architectural metrics):** 此工作表直接用於計算 SPFM (等效於 SFF) 和 LFM，是滿足此條款的核心。**稽核風險：高**。
    -   **Part 5, Clause 9 (Evaluation of violation of the safety goal due to random hardware failures):** PFH 的計算是滿足此條款的直接證據。**稽核風險：高**。
    -   **Part 9, Clause 5 (Analysis of hardware architectural metrics):** 提供了 ASIL 分解和指標計算的詳細過程。
    -   **Part 11, Annex B (IEC 61508 and ISO 26262):** 工作表名稱明確指出其計算基於 IEC 61508，稽核員會關注其與 ISO 26262 的對應性和差異。

-   **滿足 ASIL B 等級的具體要求：**
    -   **SFF (或 SPFM):** 對於 ASIL B，單點故障指標 SPFM 應 **≥ 90%**。稽核員會檢查 `AG` 列的計算結果是否都滿足此要求。
    -   **PFH:** 對於 ASIL B，每小時危險失效機率應 **< 10⁻⁷ /h**。稽核員會檢查 `AJ` 列的匯總結果是否低於此閾值。

-   **稽核員可能會問的問題：**
    1.  「請解釋暫態失效率的來源及其對 PFH 計算的貢獻。你們是如何根據 ISO 26262-5, Annex F 進行評估的？」
    2.  「數據樣本中出現了大量的 `#DIV/0!` 錯誤，請解釋根本原因，並證明在最終報告中這些錯誤已被修正。」
    3.  「公式 `AH2` 中引用的 `SafetyGoalViolations!$F$37` 代表什麼？請展示其推導過程，並說明其如何對應多點失效場景。」
    4.  「你們是如何驗證 `SUMIFS` 公式所涵蓋的範圍是完整且無遺漏的？請提供追溯性證據，將 `FMEDA` 表中的每一項失效模式鏈接到此處的區塊。」

-   **需要準備的佐證文件：**
    -   硬體設計規格書，用於解釋每個「區塊」的功能。
    -   失效率預測報告（例如 IEC TR 62380 或等效標準），用於證明基礎失效率數據的來源可靠。
    -   安全機制設計文檔，用於解釋「可檢測危險失效」的覆蓋率（DC）是如何確定的。
    -   完整的 FMEDA Excel 文件，以便稽核員可以交互式地追蹤公式和數據流。

## 6. 稽核檢查清單

此清單模擬了稽核員在審查 `BlockEval-IEC61508_opt1` 工作表時會逐項檢查的項目。

| 檢查項目 | Pass/Fail 判定標準 | 稽核發現（模擬） |
| :--- | :--- | :--- |
| **1. 數據完整性** | **Fail:** 工作表中不應存在任何計算錯誤（如 `#DIV/0!`, `#VALUE!`）。 | **Fail** - 數據樣本顯示大量計算錯誤，表明底層數據或公式存在嚴重問題。 |
| **2. SFF 計算正確性** | **Pass:** SFF 公式 `(λ_S + λ_DD) / (λ_S + λ_D)` 的實現準確無誤。<br>**Fail:** 公式邏輯與標準定義不符。 | **Pass** - 公式 `AE2` 和 `AG2` 的結構符合 IEC 61508 和 ISO 26262 對 SFF 的定義。 |
| **3. PFH 計算正確性** | **Pass:** PFH 的計算考慮了所有不可檢測的危險失效（單點和潛在）。<br>**Fail:** 遺漏了某些失效類別或錯誤地使用了相關係數。 | **Uncertain** - `AH2` 和 `AI2` 的公式結構複雜，需要追溯 `SafetyGoalViolations` 表才能確認其正確性。**高風險項**。 |
| **4. 與底層 FMEDA 的追溯性** | **Pass:** 每個區塊的失效率可以清晰、完整地追溯到底層 `FMEDA` 工作表中的失效模式。<br>**Fail:** 追溯鏈路中斷或 `SUMIFS` 範圍不完整。 | **Uncertain** - `SUMIFS` 的使用提供了追溯性，但需要手動驗證其範圍是否完整。**高風險項**。 |
| **5. 永久性/暫態性失效劃分** | **Pass:** 對永久性和暫態性失效的劃分依據合理，並有數據支持。<br>**Fail:** 劃分依據不明確或不符合行業慣例。 | **Pass** - 工作表結構清晰地區分了兩者，但其數據來源的可靠性需要進一步審查。 |
| **6. ASIL B 指標達成** | **Pass:** 所有相關區塊的 SFF ≥ 90% 且總 PFH < 10⁻⁷ /h。<br>**Fail:** 任何一項指標未達到要求。 | **Fail** - 由於計算錯誤，無法確認指標是否達成。**重大缺陷**。 |

---

### 綜合稽核結論

`BlockEval-IEC61508_opt1` 工作表在**結構和意圖**上符合 ISO 26262 的要求，展示了進行硬體架構指標量化分析的正確思路。然而，由於存在**大量的計算錯誤**，使得當前的計算結果完全無效。這構成了**重大不符合項 (Major Non-conformance)**。

**稽核風險等級：高 (High)**

**建議措施：** 必須立即修正所有計算錯誤。稽核團隊需要對 `PFH` 和 `SUMIFS` 相關的公式進行徹底的審查，以確保其邏輯的正確性和數據來源的完整性。在這些問題解決之前，無法證明硬體設計滿足 ASIL B 的定量要求。
